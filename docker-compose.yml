version: '3.8'

services:
  graph-reader:
    build: .
    ports:
      - "4000:3000"
    volumes:
      - ./src:/app/src
      - ./public:/app/public
    environment:
      - NODE_ENV=development
    command: npm run dev -- --host
